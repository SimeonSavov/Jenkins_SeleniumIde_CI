pipeline {
    agent any

    stages{
        stage("") {
            // checkout repo
            steps {
                git branch: 'main', url: 'https://github.com/SimeonSavov/Jenkins_SeleniumIde_CI'
            }
        }
        stage("") {
            // install .Net
        }
        stage("") {
            // install dependencies
        }
        stage("") {
            // build
        }
        stage("") {
            // run tests
        }
    }
}